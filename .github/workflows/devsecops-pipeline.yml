name: DevSecOps End to End Pipeline
on:
  workflow_dispatch:
jobs:
     code-quailty:
         
       
      
          uses: .github/workflow/code-quality.yml

     secrets-scan:
       
          uses: .github/workflow/secrets-scan.yml
     dependency-scan:
             uses: .github/workflow/dependency-scan.yml
     docker-scan:
         uses: .github/workflow/docker-lint.yml
